import React from "react";
import "./WeatherCard.scss";

const WeatherCard = ({ city, image, info }) => {
  return (
    <div className="weatherCard">
      <h2 className="weatherCard__title">The City</h2>
    </div>
  );
};

export default WeatherCard;
